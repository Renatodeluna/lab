'use strict';

//  ==========================================================================
//  Math Max
//  ==========================================================================

	

	// Syntax
	Math.max(value1, value2, ..., valueN);


	// Simple 
	Math.max(100, 200); // return 200
	Math.max(-1, 0);		// return 0
	Math.max(10, 10);		// return 10
	Math.max(-20, -40); // return -20
